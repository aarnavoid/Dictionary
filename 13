numbers = [10,20,10,30,30,10,40,50]
frequency = {}
for num in numbers:
    if num is frequency:
        frequency[num] +=1
    else:
        frequency[num] = 1
print("Frequency of elements:", frequency)
